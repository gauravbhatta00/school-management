# Copy to .env.desktop for local desktop-mode builds (npm run build:desktop).
# Not secret — the desktop backend (Waitress) always listens on this fixed
# loopback address.

VITE_API_URL=http://127.0.0.1:8765
