# Desktop branding assets

`icon.ico` is a **placeholder** generated for Stage 4 packaging — a plain
indigo square with the letter "E" (multi-resolution: 16/24/32/48/64/128/256).
No real icon or branding asset existed in the repository at the time Stage 4
was implemented.

Replace this file with real branding before any real distribution. Keep the
filename (`icon.ico`) and the multi-resolution `.ico` format so
`forge.config.cjs` and the BrowserWindow icon in `desktop/main.cjs` continue
to resolve it without further changes.
