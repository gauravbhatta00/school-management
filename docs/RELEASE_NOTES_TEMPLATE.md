# Release Notes — EduCore School Management System v<VERSION>

> Copy this file to `docs/releases/RELEASE_NOTES_v<VERSION>.md`, fill in every
> `<...>` placeholder, delete this note, and keep the rest of the structure —
> it exists so every release is described the same way and nothing gets
> forgotten.

## Summary

<One or two sentences: what this release is for (first pilot release,
bug-fix release, feature release, etc.).>

- **Version:** `<VERSION>` (must match the root `package.json` `"version"`
  field — see [Version management](../README.md#version-updates) in the
  README for how this propagates to the installer, the Electron app, and
  backup metadata)
- **Release date:** `<YYYY-MM-DD>`
- **Installer:** `SchoolManagementSetup.exe`
- **Installer SHA-256:** `<paste from SchoolManagementSetup-<VERSION>.exe.sha256,
  generated by `npm run verify:release`>`

## What changed

<Bullet list of user-facing changes since the previous release. Group by
area if there are more than a handful:>

- **Backend:**
- **Frontend / UI:**
- **Backup & restore:**
- **Other:**

## Upgrade notes

<Anything an existing installation's admin needs to know before or after
installing this version. If nothing applies, write "None — this release
is safe to install over a previous version with no manual steps.">

- Pending database migrations this release introduces (if any): `<list, or "None">`
- Any manual action required before installing: `<steps, or "None">`
- Any manual action required after installing: `<steps, or "None">`

**Before upgrading an existing installation, always take a manual backup
first** (Settings → Create Backup, or `Open Backup Folder` to confirm one
already exists from today). This is true for every release, migration or
not — see [docs/PILOT_CHECKLIST.md](PILOT_CHECKLIST.md).

## Known issues

<Anything observed but not fixed in this release. If none, write
"None known.">

## Verification performed before this release

Check off what was actually done — do not check a box that wasn't run for
*this* release:

- [ ] `npm run verify:release` completed successfully (build, package,
      installer, checksum all produced with no errors)
- [ ] Backend test suite passed (`cd backend && pytest`)
- [ ] Frontend lint passed (`cd frontend && npm run lint`)
- [ ] Manual backup creation tested in the installed app
- [ ] Restore tested end-to-end in the installed app (see
      [docs/PILOT_CHECKLIST.md](PILOT_CHECKLIST.md))
- [ ] Clean install tested on a machine/profile with no prior app data
- [ ] Upgrade-over-previous-version tested (existing data survives, any
      pending migration applies cleanly)
- [ ] Uninstall tested (app removed, user data under
      `%APPDATA%\school-management-desktop\` left untouched)

## Files changed

<Optionally link to the commit range or PR for this release, e.g.:
`git log <previous-tag>..<this-tag> --oneline`>

## Suggested Git tag

```
git tag -a v<VERSION> -m "EduCore v<VERSION>"
```

(Tagging is a manual, explicit step — nothing in this project's tooling
tags or publishes a release automatically.)
